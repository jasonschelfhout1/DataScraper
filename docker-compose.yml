services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: datascraper
      POSTGRES_USER: datascraper
      POSTGRES_PASSWORD: datascraper
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
